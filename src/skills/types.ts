// Shared skills layer (Build-Spec §7). Thin agents call these reusable skills.
// All are stubs with stable signatures; implement against real APIs in later phases.
export interface Source { title: string; url: string }
