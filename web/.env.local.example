# The dashboard reads the pipeline's secrets from the repo-root ../.env automatically.
# Only set values here to OVERRIDE that (e.g. point the dashboard at a different Supabase).
# SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=
